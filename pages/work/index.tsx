import React from "react";
import Layout from "../../components/layout/Layout";

const work = () => {
    return (
        <Layout>
            <div>WORK</div>
        </Layout>
    );
};

export default work;
